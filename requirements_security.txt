# AGL Security Tool — المتطلبات الأساسية
# Core Dependencies

# Required
requests>=2.28.0

# Formal Verification (Z3 SMT Solver)
z3-solver>=4.12.0

# Optional — External tool integration
# pip install slither-analyzer    # Slither static analysis
# pip install mythril             # Mythril symbolic execution
# pip install semgrep             # Semgrep pattern matching
