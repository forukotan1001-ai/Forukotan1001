AGL Cohesion 100/100 — Golden Snapshot

المتغيرات المستعملة:

- AGL_REASONER_MODE=dkn
- AGL_QUANTUM_MODE=simulate
- AGL_TEST_SCAFFOLD_FORCE=1

مفاتيح الـregistry المُضافة / المؤكدة (موجودة الآن):

- config
- logger
- core_services
- safety_center
- memory_store
- communication_bus
- task_orchestrator
- output_formatter
- dkn_bus
- knowledge_graph
- meta_orchestrator
- rag
- hybrid_composer
- planner_agent
- retriever

سطر تشغيل الفحص (cohesion):
PowerShell (fast):

$env:AGL_REASONER_MODE='dkn'
$env:AGL_QUANTUM_MODE='simulate'
$env:AGL_TEST_SCAFFOLD_FORCE='1'
py -3 .\scripts\integration_cohesion_check.py

2) فصل “وضع الاختبار” عن الإنتاج

راية AGL_TEST_SCAFFOLD_FORCE=1 جيدة للاختبارات ويجب عدم تفعيلها في الإنتاج.

للإنتاج (نظيف):
$env:AGL_REASONER_MODE='dkn'
$env:AGL_QUANTUM_MODE='simulate'
$env:AGL_TEST_SCAFFOLD_FORCE=$null
py -3 .\scripts\integration_cohesion_check.py

للاختبارات (CI-only): فعّلها داخل سكربت CI فقط؛ لا تتركها معرفة في بيئة الإنتاج.

3) خطوات سريعة لرفع الذكاء العملي

A) تفعيل ذاكرة دلالية خفيفة بدل TF-IDF

- متغيّر اقتراحي: AGL_RETRIEVER_MODE (قيمة افتراضية 'tfidf', لاحقًا 'embed').
- خارطة ترقية: add an embedding-based retriever script to build local index.

B) فرض مخرجات JSON منظّمة افتراضيًا

- تأكد أن استدعاءات `Hosted_LLM` تمرُّ عبر `parse_json_or_retry`.
- سجّل النتائج المنظمة في: `artifacts/reports/structured_outputs.jsonl`.
- أمر تحقق سريع (واحد من حالات الاختبارات):

py -3 -m pytest -q tests/test_agi_advanced_eval.py::test_agi_advanced__case1_multidomain -s --maxfail=1

C) تفعيل التعلم الوزني في DKN

- راية مفيدة: AGL_DKN_LEARNING_RATE (مثال: '0.1').
- التأكد: `MetaOrchestrator.update_weights()` تُستدعى بعد كل إجماع ومعها feedback من Rubric_Enforcer.

4) مقترح CI نظيف بمرحلتين

Mock/Smoke (سريع):
$env:AGL_EXTERNAL_INFO_MOCK='1'
$env:AGL_TEST_SCAFFOLD_FORCE='1'
py -3 .\scripts\integration_cohesion_check.py

Live (اختياري/ليلي):

- لا تفعّل AGL_TEST_SCAFFOLD_FORCE
- سجّل artifacts الموجود في `artifacts/` (episodic.jsonl, lessons.jsonl, structured_outputs.jsonl)

5) اختبار تراجعي قبل إزالة الـFORCE

$env:AGL_TEST_SCAFFOLD_FORCE=$null
py -3 -m pytest -q tests/test_agi_advanced_eval.py::test_agi_advanced__case1_multidomain -s --maxfail=1

إذا الفرق كبير: قرب "قائمة المتطلبات" (checklist) إلى مقدمة الـprompt الديناميكي وفكّر في تفعيل Rubric_Enforcer فقط في CI لإعادة الصياغة.

6) قائمة قصيرة لما يلي مباشرة

- توثيق المتغيرات في docs/CONFIG.md
- إضافة أمر: scripts/ci_smoke.ps1 يشغّل الـsmoke بـ FORCE فقط
- تسجيل الذاكرة: تأكد من كتابة episodic.jsonl و lessons.jsonl مع كل إجماع
- طباعة مفاتيح الـRegistry لمرة واحدة في اللوج عند الإقلاع (Debug)

اختر التالي لرفع الأداء المعرفي:

1) تقوية الـprompt الديناميكي بقائمة كلمات مفتاحية "لازمة النجاح" (أضيفها في Composer)
2) تفعيل retriever='embed' مع sentence-transformers وخطوات بناء الفهارس المحلية

---
Generated by the cohesion run and snapshot process.
